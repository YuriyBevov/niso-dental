block variables
  - modalId = ''
  - modalClasses = ''

.modal-overlay
  .modal.main-modal(id= modalId class= modalClasses)
    .modal-wrapper
      button(type="button" aria-label="Закрыть").modal-closer
        svg(width="14" height="14")
          use(xlink:href="./assets/sprite.svg#icon-close")
      if modalHeader
        .modal-header
          block modalHeader
      .modal-content
        block modalContent
