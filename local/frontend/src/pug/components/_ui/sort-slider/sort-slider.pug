mixin sort-slider(data)
  .swiper.sort-slider
    .swiper-wrapper
      each tag in data
        .swiper-slide
          +main-btn({
            tag: 'a',
            href: '#',
            text: tag.title,
            cls: tag.cls
          })