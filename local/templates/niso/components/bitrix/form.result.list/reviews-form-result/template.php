<?php
if (!defined("B_PROLOG_INCLUDED") || B_PROLOG_INCLUDED !== true) die();

debug($arResult);

?>



<?/*
foreach ($arResult["arrResults"] as $arrResult) {
	if ($arrResult["STATUS_TITLE"] == "Размещен на сайте") {
		foreach ($arResult["arrAnswers"] as $arItem) {
			foreach ($arItem as $answer) {
				foreach ($answer as $field) {
					debug($field);
				}
			}
		}
	}
} */ ?>